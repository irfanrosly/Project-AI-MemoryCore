# 2026-07-06

## video-player block (sb-custom-blocks / xsolla-com)
Built new block from approved plan (continued from prior session), then added dark/light mode toggle.

- First xsolla-com video block. 2 fields: video URL + thumbnail image. Thumbnail on load → click plays inline (not modal).
- Auto-detect source: YouTube/Vimeo → iframe embed; direct file → native `<video>`; invalid → stays on thumbnail. `isSafeUrl` guards javascript:/data:.
- Dark/light: matched steps pattern — `theme` enum, `lightTheme`/`darkTheme` class, `@xsolla-com/common/theme` vars. Default 'dark' (kept original look). Play triangle black on cyan circle, theme-independent.
- Verified: prettier + lint clean, client+SSR build green (SSR 66 modules — no unguarded browser APIs).
- NOT committed, no Jira ticket. Manual SiteBuilder QA still pending.

Smooth session — Irfan happy, wrapped clean.
