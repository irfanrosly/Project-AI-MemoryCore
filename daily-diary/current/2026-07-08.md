# Diary — 2026-07-08

## xsolla-com Block Catalog → Confluence

Built reference page listing all 18 xsolla-com SiteBuilder blocks: name + version + plain-language description. Goal: single place for editors/PMs/devs to pick right block.

- Data source: `version` field in each `projects/xsolla-com/*/package.json` on `xsolla-com-main` branch (source version, not deployed). Excluded `@xsolla-com/common` (shared pkg, not a block).
- Published via Atlassian MCP → Confluence space XSOL, page id `24840175707`.
  URL: https://xsolla.atlassian.net/wiki/spaces/XSOL/pages/24840175707/xsolla-com+Block+Catalog

### Gotcha worth remembering
`updateConfluencePage` on an existing empty draft failed: `Version number must be 1 when publishing... Provided version: [2]`. The update tool auto-increments (draft=v1, tool sends v2) and has no version param. Fix: use `createConfluencePage` for a fresh published page instead of publishing-from-draft. Left old blank draft `24839979115` unused.

### Data-quality notes surfaced on the page
- Featured Articles version typo: `1.0.2-wesbite-4610.0` ("wesbite" misspelled ticket prefix).
- 6 blocks on unstable pre-release versions (event-list, featured-articles, metrics, product-spotlight, products-grid, steps).
- common-showcase is a dev demo block, not real landing content.

## "Codebase setup" Confluence page → added MR section

Added new **§10 Creating a Merge Request** to "Codebase setup" page (`24450957353`, space XSOL). GitLab UI click-steps: New MR → source ticket branch → target `main` → title `[XX-1234] ...` → assign reviewer → label `ready to merge` → create → maintainer merges. Positioned after §9 Releasing (natural next step: after QA + stable release). Renumbered §10/11/12 → §11/12/13 (Branching / Shared Libraries / Troubleshooting). Source of truth for flow: repo `docs/GITFLOW.md`.

- Page now version 4. Verified via getConfluencePage.
- URL: https://xsolla.atlassian.net/wiki/spaces/XSOL/pages/24450957353/Codebase+setup
- Note: this was an update to an already-published (`current`) page, so the draft-publish v1 gotcha did NOT apply — updateConfluencePage cleanly bumped v3→v4.

## Per-block doc pages → gold-standard template + hero drift fix

Raised products-grid page (`24840011951`, space XSOL) to a gold-standard block-doc template: fixed version inconsistency (header now cites both latest-release + source package.json version), removed duplicate legacy `## Analytics` paragraph, deepened schema reference to full nesting (block → row → card → button/badge/background), rewrote reader-facing "What it does", locked canonical section order. That page shape became the template for the other 14.

Then swept the remaining 14 xsolla-com per-block pages for consistency. Hero (`24840175863`) was the sole drift:
1. Single-line `·`-separated header → reformatted to 3-line template layout (Package / Latest release · Source / Source path).
2. Broken schema-table row: `title` default `'Sell PC games\ndirectly to players'` — the `\n` shattered the markdown table.

### Gotcha worth remembering — Confluence markdown table-cell newline bug
ANY `\n` in a table cell breaks the row — including a doubled `\\n`. The markdown→storage parser converts the escape to a real newline anyway, splitting the row into garbled halves. My first hero fix (v5) escaped as `\\n` assuming literal render; verification showed it still broke. Fix (v6, verified): drop the newline entirely, use two code spans + slash separator — `` `'Sell PC games'` / `'directly to players'` `` — matching the existing seeded-button-row idiom. Cost 2 writes instead of 1. Rule: never put `\n` in a Confluence markdown table cell.

Hero now v6, verified rendering intact. **All 15 xsolla-com per-block Confluence pages consistent with the golden template.**
