# 2026-07-16 — Diary

## Onboarding docs for the two new grads (sb-custom-blocks / xsolla-com)
Irfan's getting two fresh-grad frontend devs and asked me to design the best way to onboard them fast. Ran 3 parallel Explore agents (docs audit, codebase map, gotchas). Findings: the repo's reference docs are genuinely good; what's missing is connective tissue + 4 gap-fillers (credentials/`.npmrc` incompleteness, federated-module mental model, contradictory Node/pnpm versions, no distributed day-1 path).

Built (under `projects/xsolla-com/docs/`, per Irfan's call to scope them to the team):
- `ONBOARDING.md` — 5-phase learn-by-building program, reading order, gap-fillers, glossary, known-traps.
- `TEXT-AND-IMAGE.md` — explains the `useBlockSelector`/`TextEditor`/`useEditableText`/`useResolveImage` cluster using real cta-module code. Core idea: schema stores *refs* (`L:`/`I:`), the other hooks *resolve* them. Fills a real gap (BLOCK-ANATOMY never documents TextEditor/useEditableText).
- `create-block-skill/` — copy of my local create-block skill so it reaches a fresh clone.
- `projects/xsolla-com/README.md` — pointer.

## ⚠️ Incident: untracked docs got wiped
At session wrap, discovered all the doc files were GONE from disk. Cause: they were untracked (never committed), and today's branch/demo shuffling (reflog: WEBSITE-4775 → Irfan/demo → xsolla-com-main → WEBSITE-4562, with demo commits) cleaned the working tree at 09:54. Untracked = unrecoverable from git. Recovered fully by rewriting from my session context + re-copying the skill from ~/.claude/skills. **Lesson: commit generated docs immediately, or they die on the next `git clean`/branch dance.** Left them untracked again + flagged to Irfan to commit on a dedicated branch (current branch WEBSITE-4562 has an active merge conflict).

## Still open
- Onboarding docs uncommitted (need dedicated branch — WEBSITE-4562 is mid-merge-conflict).
- video-player WEBSITE-4775 got committed/released earlier (reflog shows release tag) — that work is safe.
