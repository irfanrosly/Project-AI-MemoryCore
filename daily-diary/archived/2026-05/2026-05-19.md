# Session Diary — 2026-05-19

## Project: xsolla-com/event-list block

### What we did

Fixed mobile (360px) layout for the `event-list` block to match Figma.

**Core problem**: Arrow button needed to be top-right of logo at mobile, but inside content group at tablet+. CSS can't move DOM — solved with two arrow elements toggled via `display: none`.

**Changes in `block.tsx`**:
- Added `logoRow` wrapper containing `logoBox` + `mobileArrow` (new)
- Removed `nameDateSpacer` div
- `arrowCol` (tablet+ arrow) stays inside `eventContent`, hidden at mobile

**Changes in `block.css.ts`**:
- `eventCard`: mobile padding 12px, gap 12px
- `headlineControls`: mobile padding 12px
- `logoBox`: fixed width 200px × 120px at mobile
- New `logoRow`: flex row, space-between at mobile → auto-width at tablet
- New `mobileArrow` + `mobileArrowIcon`
- `nameDateGroup`: gap 40px at mobile (replaced spacer div), tablet uses justify-between
- `eventContent`: removed paddingTop (card gap handles spacing)
- `arrowCol`: `display: none` at mobile, flex at tablet+
- All font sizes mobile-first (no separate mobile breakpoint)

**Linter note**: Auto-removed `pilatBook`/`pilatDemi` imports, replaced `eventLocation`/`eventName` fontFamily with `pilatRegular`. May need design review.

### Also done
- Listed all `@xsolla-*` packages in repo root monorepo

### Pending follow-up
- Verify linter font family changes are correct for design (pilatDemi → eventLocation, pilatBook → eventName)
